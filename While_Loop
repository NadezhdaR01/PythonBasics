# исходные данные
my_list = [42, 69, 322, 13, 0, 99, -5, 9, 8, 7, -6, 5]

i = 0  # переменная для хранения индекса
#цикл while для прохождения по элементам списка
while i != len(my_list):
    # проверяем элементы списка, если < 0, то прерываем цикл,
    # если = 0, то пропускаем, иначе выводим в консоль
    if my_list[i] < 0:
        break
    elif my_list[i] == 0:
        i +=1
        continue
    else:
        print(my_list[i])
        i +=1  # Увеличиваем индекс на 1, чтобы перейти к следующему элементу

